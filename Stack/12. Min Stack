Design a stack that supports push, pop, top, and retrieving the minimum element in constant time.

Implement the MinStack class:

MinStack() initializes the stack object.
void push(int val) pushes the element val onto the stack.
void pop() removes the element on the top of the stack.
int top() gets the top element of the stack.
int getMin() retrieves the minimum element in the stack.
You must implement a solution with O(1) time complexity for each function.

//Hint: 
- for getMin, just push value iff value <= getMin[-1]
-- if we're popping the smallest value from stack, we pop it from getMin also

var MinStack = function() {
    this.stack = [];
    this.minValStack = [];
};

MinStack.prototype.push = function(val) {
    this.stack.push(val);
    if (this.minValStack.length === 0 || val <= this.minValStack[this.minValStack.length - 1]) {
        this.minValStack.push(val);
    }
};

MinStack.prototype.pop = function() {
    const popped = this.stack.pop();
    if (popped === this.minValStack[this.minValStack.length - 1]) {
        this.minValStack.pop();
    }
};

MinStack.prototype.top = function() {
    return this.stack[this.stack.length - 1];
};

MinStack.prototype.getMin = function() {
    return this.minValStack[this.minValStack.length - 1];
};
